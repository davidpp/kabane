<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Scheduled pull for one Cabane device (macOS launchd).

  Install:  sed "s|__HOME__|$HOME|g" com.cabane.sync-pull.plist > ~/Library/LaunchAgents/com.cabane.sync-pull.plist
            launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.cabane.sync-pull.plist
  Remove:   launchctl bootout gui/$(id -u)/com.cabane.sync-pull

  `cabane` is the bun-linked binary at ~/.bun/bin/cabane; PATH below includes it.
  A pull writes rows and may relabel colliding short ids, which is why it is
  scheduled rather than run inside an interactive command.
-->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.cabane.sync-pull</string>

    <key>ProgramArguments</key>
    <array>
        <string>__HOME__/.bun/bin/cabane</string>
        <string>sync</string>
        <string>pull</string>
    </array>

    <key>StartInterval</key>
    <integer>300</integer>

    <key>RunAtLoad</key>
    <true/>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>__HOME__/.bun/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
        <key>CABANE_HOME</key>
        <string>__HOME__/.cabane</string>
    </dict>

    <key>StandardOutPath</key>
    <string>__HOME__/.cabane/sync-pull.log</string>
    <key>StandardErrorPath</key>
    <string>__HOME__/.cabane/sync-pull.error.log</string>
</dict>
</plist>
