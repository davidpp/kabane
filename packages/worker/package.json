{
	"name": "cabane-worker",
	"version": "0.1.0",
	"description": "Cabane hub: the sync log Durable Object and the cloud device serving MCP",
	"license": "MIT",
	"private": true,
	"type": "module",
	"scripts": {
		"test": "tsc --noEmit && vitest run",
		"test:watch": "vitest",
		"typecheck": "tsc --noEmit",
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"clean": "rm -rf node_modules .wrangler"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "0.22.0",
		"@cloudflare/workers-types": "5.20260901.1",
		"@types/bun": "1.4.0",
		"typescript": "5.9.3",
		"vitest": "4.1.11",
		"wrangler": "4.128.0"
	},
	"dependencies": {
		"@cabane/core": "workspace:*",
		"jose": "6.2.10",
		"zod": "3.25.76"
	}
}
